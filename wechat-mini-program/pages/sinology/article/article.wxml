<!--pages/sinology/article/article.wxml-->

<view class="article-page">

  <view class="title">{{title}} {{chapter}} {{section}}</view>
  <view class="author">{{author}}</view>
  <view wx:for="{{paragraphs}}" wx:key="{{item.title}}" class="{{isParagraph==true ? 'left' : 'center'}}-content">{{item}}</view>

</view>